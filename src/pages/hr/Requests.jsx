import React from "react";

const Requests = () => {
    return (
        <div>
            <h1 className="text-2xl font-bold">Employee Asset Requests</h1>
            {/* পরে এখানে HR এর request table দেখানো হবে */}
        </div>
    );
};

export default Requests; // ✅ Default export
